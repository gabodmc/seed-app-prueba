{
  "name": "seed-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deliberately flawed multi-tenant SaaS used as ground truth for leakage measurement.",
  "scripts": {
    "prepare-db": "prisma migrate deploy && node src/seed.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
